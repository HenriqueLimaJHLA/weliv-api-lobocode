# Nginx (legado)

O fluxo oficial do template para VPS multi-projeto agora usa Traefik como gateway compartilhado, sem comandos de deploy via Nginx.

Use:

- `./scripts/deploy.sh vps-gateway`
- `./scripts/deploy.sh vps-app`

Guia atualizado:

- `docs/VPS_MULTI_PROJECT.md`
